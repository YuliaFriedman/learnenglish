import { StyleSheet } from "react-native";

export const draggableStyle = StyleSheet.create({
  draggable: {
    backgroundColor: '#4caf50',
    justifyContent: 'center',
    display: "flex",
    height: "100%"
  }
});
