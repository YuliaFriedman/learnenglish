import { StyleSheet } from "react-native";

export const AppHeaderStyling = StyleSheet.create({
  host: {
    display: "flex",
    flexDirection: "row"
  },

  title: {
    flex: 1,
    textAlign: "center"
  },

  homeButton: {
    backgroundColor: "red"
  }
})
